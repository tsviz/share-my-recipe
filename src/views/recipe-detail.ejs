<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8f9fa; }
        .favorite-heart { color: #e25555; font-size: 2rem; }
        .favorite-heart-outline { color: #ff69b4; font-size: 2rem; text-shadow:0 1px 4px #fff,0 0 6px #ffb6c1; }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-3 d-flex align-items-center">
            <%= recipe.title %>
            <% if (typeof recipe.isFavorited !== 'undefined' && user) { %>
                <form method="POST" action="/recipes/<%= recipe.id %>/<%= recipe.isFavorited ? 'unfavorite' : 'favorite' %>" style="display:inline; margin-left: 1rem;">
                    <button type="submit" class="btn p-0 border-0 bg-transparent" title="<%= recipe.isFavorited ? 'Unfavorite' : 'Favorite' %>">
                        <% if (recipe.isFavorited) { %>
                            <span class="favorite-heart">&#10084;&#65039;</span>
                        <% } else { %>
                            <span class="favorite-heart-outline">&#9825;</span>
                        <% } %>
                    </button>
                </form>
            <% } %>
        </h1>
        <p class="lead"><%= recipe.description %></p>
        <h4>Ingredients</h4>
        <ul>
            <% recipe.ingredients.forEach(ingredient => { %>
                <li><%= ingredient.ingredient_name %> <% if (ingredient.quantity) { %>(<%= ingredient.quantity %>)<% } %></li>
            <% }) %>
        </ul>
        <h4>Instructions</h4>
        <p><%= recipe.instructions || 'No instructions available.' %></p>
        <div class="mt-4">
            <% if (typeof backButton !== 'undefined' && backButton && backButton.text && backButton.text.includes('search')) { %>
                <!-- Special handling for search back button with options -->
                <div class="btn-group" role="group">
                    <button type="button" class="btn btn-outline-secondary" onclick="goBackToSearch('original')">
                        <span style="margin-right: 8px;">🔍</span>
                        Back to Original Search
                    </button>
                    <button type="button" class="btn btn-outline-secondary" onclick="goBackToSearch('new')">
                        <span style="margin-right: 8px;">🔍</span>
                        New Search
                    </button>
                </div>
                
                <script>
                function goBackToSearch(type) {
                    if (type === 'original') {
                        // Try to go back in history first
                        if (window.history.length > 1) {
                            window.history.back();
                        } else {
                            // Fallback to the search URL
                            window.location.href = '<%= backButton.url %>';
                        }
                    } else if (type === 'new') {
                        // Open new search in current tab
                        window.location.href = '/recipes/search';
                    }
                }
                </script>
                
            <% } else if (typeof backButton !== 'undefined' && backButton) { %>
                <a href="<%= backButton.url %>" class="btn btn-outline-secondary">
                    <span style="margin-right: 8px;"><%= backButton.icon %></span>
                    <%= backButton.text %>
                </a>
            <% } else { %>
                <a href="/recipes" class="btn btn-outline-secondary">📖 Back to Recipes</a>
            <% } %>
            
            <% if (user && user.id === recipe.user_id) { %>
                <a href="/recipes/edit/<%= recipe.id %>" class="btn btn-outline-primary ms-2">✏️ Edit Recipe</a>
            <% } %>
        </div>
    </div>
</body>
</html>
