<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Recipe Sharing Platform</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-image: url('https://images.unsplash.com/photo-1543353071-10c8ba85a904?auto=format&fit=crop&w=1500&q=80');
            background-size: cover;
            background-attachment: fixed;
            background-repeat: no-repeat;
            min-height: 100vh;
        }
        .auth-container {
            background-color: rgba(255, 255, 255, 0.92);
            border-radius: 1rem;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
            padding: 2rem;
            margin-top: 5rem;
        }
        .form-control:focus, .btn:focus {
            box-shadow: 0 0 0 0.25rem rgba(255, 140, 0, 0.25);
            border-color: #ff8c00;
        }
        .btn-main {
            background: linear-gradient(90deg, #ffb347 0%, #ffcc33 100%);
            color: #fff;
            border: none;
            box-shadow: 0 2px 8px rgba(255, 140, 0, 0.15);
            transition: all 0.2s;
        }
        .btn-main:hover {
            background: linear-gradient(90deg, #ffcc33 0%, #ffb347 100%);
            transform: translateY(-1px);
        }
        .food-icon {
            font-size: 2rem;
            margin-right: 0.5rem;
        }
        .alert-warning {
            background-color: #ffecb3;
            border-color: #ffcc33;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-5 col-md-7">
                <div class="auth-container">
                    <div class="text-center mb-4">
                        <h1 class="h3"><span class="food-icon">🍽️</span>Recipe Sharing</h1>
                        <p class="text-muted">Welcome back! Please login to continue.</p>
                    </div>
                    
                    <% if(messages.error) { %>
                        <div class="alert alert-warning">
                            <%= messages.error %>
                        </div>
                    <% } %>
                    
                    <% if(messages.redirect) { %>
                        <div class="alert alert-info">
                            <%= messages.redirect %>
                        </div>
                    <% } %>
                    
                    <form action="/login" method="POST">
                        <input type="hidden" name="returnTo" value="<%= typeof returnTo !== 'undefined' ? returnTo : '' %>">
                        
                        <%/* Visually indicate if there's a redirect destination */%>
                        <% if(typeof returnTo !== 'undefined' && returnTo) { %>
                            <div class="mb-3 small text-muted">
                                <i class="bi bi-arrow-return-right"></i> 
                                You'll be redirected back to your previous page after login.
                            </div>
                        <% } %>
                        <div class="mb-3">
                            <label for="email" class="form-label">Email address</label>
                            <input type="email" class="form-control" id="email" name="email" required>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>
                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-main btn-lg">Login</button>
                        </div>
                    </form>
                    
                    <div class="text-center mt-3">
                        <p>Don't have an account? <a href="/register">Register</a></p>
                    </div>
                    
                    <div class="text-center mt-4">
                        <a href="/" class="btn btn-sm btn-outline-secondary">Back to Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>